openapi: 3.0.3
info:
  title: Sample API
  version: 1.0.0

paths:
  /users:
    $ref: './paths/users.yaml'
  /pets:
    $ref: './paths/pets.yaml'

components:
  schemas:
    User:
      $ref: './components/schemas/User.yaml'
  responses:
    NotFound:
      $ref: './components/responses/NotFound.yaml'
